/**
 * TESTING MODULE
 * 
 * - Ways to test :
 *      1. Using default test data
 *          -> pick a date, figure out the schedule (perhaps by hand so it's precise ?), get API result for date, compare
 *          -> pick date interval, repeat as above
 *      2. Web testing
 *          -> controller / context testing
 */

public class RestControllerTest {


    // @Test
    /*
    public void connectionEstablished() throws Exception {
        assertThat(controller).isNotNull();
    }
    */
    
}
